{
  "name": "serverless-felix",
  "version": "1.0.0",
  "description": "Serverless Cloud Project",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "predeploy": "npm run build && cp package.json package-lock.json dist/",
    "deploy:dev": "npm run predeploy && cd dist && gcloud functions deploy hello --gen2 --runtime=nodejs20 --region=us-central1 --source=. --entry-point=hello --trigger-http --allow-unauthenticated --project=serverless-felix-dev --set-build-env-vars=GOOGLE_NODE_RUN_SCRIPTS=",
    "deploy:prd": "npm run predeploy && cd dist && gcloud functions deploy hello --gen2 --runtime=nodejs20 --region=us-central1 --source=. --entry-point=hello --trigger-http --allow-unauthenticated --project=serverless-felix-prd --set-build-env-vars=GOOGLE_NODE_RUN_SCRIPTS="
  },
  "author": "Felix Stavonhagen",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "serverless": "^3.38.0",
    "serverless-google-cloudfunctions": "^4.6.0",
    "serverless-plugin-typescript": "^2.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
